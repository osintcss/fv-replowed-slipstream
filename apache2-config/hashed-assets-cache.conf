<IfModule mod_headers.c>
    # The final filename segment is a content hash. These immutable assets
    # are safe to cache aggressively; all other game routes retain Apache's
    # normal cache behavior.
    <Location "/farmville/assets/hashed/">
        Header always set Cache-Control "public, max-age=31536000, immutable"
    </Location>
</IfModule>
